class test1 {
    
    test15() {
        def x = 10;
        def v1 = 5;
        def u = 1;
        def v = 2;

        x = 0;
        v1 = 2*x + 10;
        x = u+v;
    
        v1 = 40*x + 10;
        x = u + v;
        
        def a = 5;
        def b = 9;
        def c = b + 20; // y - 1 * z > k3
        def d = 10 * (c+a) + 7; // x - k1 * (y[+u]) > k2
        
        //then we can infer the constraint x-k1 *(z[+u]) > k2+k1 * (k3 + 1)
        // d - 10*(b+a) > 6 + 10*(19+1) = 206
    }
}
